# 2026-09-08

## WCL AI Analyzer 架构审计（Phase 1，只读，未改代码）

- 产出 `docs/architecture-audit.md`（12 节：结构/技术栈/数据流/WCL 链/分析流/AI 流/MCP/测试/与 ARCHITECTURE.md
  差异/优先级/10 阶段迁移计划/风险）。**等待用户确认后再进入第二阶段**。
- ~~pnpm 不能用~~ **更正**：pnpm v12.3.4 在 **PowerShell** 下完全正常（install/build/test/typecheck/lint 全绿）。
  只有 **Git Bash** 里 corepack shim 路径转换失败（`/c/develop/...` → `C:\c\develop\...`）。
  Git Bash 下的等价命令：`node "C:/develop/env/nvm/nodejs/node_modules/corepack/dist/pnpm.js" <args>`。
  以后 pnpm 相关操作**优先用 PowerShell 工具执行**。
- 核心差距（目标架构 vs 现状）：
  1. **无 Combat Facts 层** —— `metrics: Record<string, unknown>`，analyzer 直接遍历 `CombatEvent[]` 出指标。
  2. **无 Spec Knowledge** —— 知识是 `analysis-engine/src/specs/*/constants.ts` 里硬编码的 abilityId + cooldownMs，
     无 priority / patch / source / confidence；`knowledge/` 目录完全不存在。
  3. **无 Verdict 五档** —— 规则是"阈值检查 → 直接判错"，severity 即结论。
  4. Finding 缺 `confidence/expected/actual`；Evidence 缺 `fightId/eventId`，大量 evidence 是聚合值非时间点。
  5. 无 Patch Resolver；版本化只有 `ANALYSIS_VERSION = 1`（且 `setAnalysis/getAnalysis` 零调用，分析结果从不缓存）。
- 读码验证的具体缺陷（不是推测）：
  - **死亡分析恒空**：`getPlayerDeaths` 用 `sourceId=playerId` 拉 `Deaths`，`DeathAnalyzer` 却筛 `targetId===player.id`；
    且死亡前伤害窗口需要 `DamageTaken`，但 `loadPlayerEvents` 只拉了 `DamageDone`。
  - **Debuffs 数据源未拉取**（只拉 `Buffs`），debuff uptime 规则依赖 applydebuff 出现在 Buffs 结果里，需实连验证。
  - MCP 的 `get_player_casts|buffs|damage` 会把**最多 100,000 条原始事件**回吐给宿主 AI，与 ARCHITECTURE §34 冲突。
  - `metrics` 是扁平 record，`metrics.spec` 会被 `Object.assign(specResult.metrics)` 覆盖。
- 已做对的部分（不要回退）：GraphQL 100% 收敛在 wcl-client；MCP/Web 不直连 GraphQL；AI 不自己统计；
  token 不外泄；排行榜 RankingReference 基线；会话状态机在 apps/web。
- 下一步候选（待确认）：阶段 A 领域模型（纯新增）→ 阶段 C Combat Facts（先用 BM 一个专精打通，旧链路并行）。

## 重构执行：阶段 A / B / C 已完成（用户确认"按计划执行"）

- **本环境 Git Bash 下 pnpm 不可用**（corepack shim 路径转换 bug），但 **PowerShell 下正常（v12.3.4）**。
  已用 PowerShell 跑通真实工具链：`pnpm install`（11 workspace projects）、`pnpm build`（10 个包按拓扑序）、
  `pnpm test`（34 文件/178 用例）、`pnpm typecheck`、`pnpm lint` —— **全部通过**。
  手工建的 `node_modules/@wcl/*` 软链与 pnpm 生成的一致，已被 pnpm 接管，无需清理。
  Git Bash 备用：`npx tsc -p packages/<pkg>/tsconfig.json` + `npx vitest run`。
- 阶段 A（领域模型，纯新增）：
  - `packages/domain/src/fact.ts`（CombatFact / CastFacts / CooldownFacts / BuffFacts / ResourceFacts /
    GcdFacts / FactSet / listFacts）
  - `packages/domain/src/knowledge.ts`（KnowledgeSource / SpecKnowledgeRef / CONFIDENCE_SCALE）
  - `analysis.ts` 增 `Verdict` 五档、`MIN_MISTAKE_CONFIDENCE=0.6` + `canEscalateToMistake()`、
    Finding 增 `confidence/verdict/expected/actual`、Evidence 增 `fightId/eventId/note`、
    新增 `AnalysisVersion`（analyzerVersion + knowledgeVersion）
  - `tests/domain.test.ts` 6 用例
- 阶段 B：新建 `packages/combat-normalizer`（normalize.ts 从 wcl-client 迁入）；
  `packages/wcl-client/src/event-normalizer.ts` 改为 `@deprecated` re-export；测试文件整体迁移（非删除）。
- 阶段 C：新建 `packages/combat-facts`——timeline 原语 + computeCastFacts / computeGcdFacts /
  computeCooldownFacts / computeBuffFacts / computeResourceFacts / buildCombatFacts；
  新增 `CooldownDelaySample{timestamp, idealTimestamp, delayMs}`（CD 延迟证据的关键：expected vs actual 时间点）。
  analysis-engine 的 casts/gcd/cooldowns/buffs/resources 五个 Analyzer 改为
  `toFactsInput(context)` → 消费 Facts → 显式映射回**完全相同的 metrics 结构**（旧测试零改动全过）。
  `core/timeline.ts` 改为 re-export combat-facts（单一实现）。
- 验证：34 文件 / 178 用例全绿（原 30/162 → +16 facts 用例）；9 个包+2 个 app 全部 tsc 通过；eslint 通过。
- 待办（下一阶段）：D Spec Knowledge、E 优先级求值器 + Verdict、F Finding/Evidence 升级、
  G ai-reasoning、H MCP 对齐、I 双版本号、J fixture 重构。
- **未执行的已知缺陷修复**（留到对应阶段）：死亡分析 source/target 不匹配 + 缺 DamageTaken；
  Debuffs 数据源未拉取；MCP 原始事件出口无上限；metrics.spec 覆盖风险。

## 阶段 D — Spec Knowledge 包完成

- 新建 `packages/spec-knowledge`：
  - `src/types.ts`：AbilityKnowledge（key/abilityId/kind/cooldownMs/minCastRatio）、BuffKnowledge
    （buff|debuff + appliesTo:self|enemy + expectedUptime）、ResourceKnowledge、CooldownKnowledge、
    **PriorityRule（Condition→Action：when{targetCountMin/Max, resourceMin/Max, buffActive/Missing,
    cooldownReady, fightPhase} + action + confidence + rationale）**、SpecKnowledge（extends domain
    SpecKnowledgeRef，带 sources + effectiveFrom/To）
  - `src/registry.ts`：SpecKnowledgeRegistry —— 按 specId → 回退 specName；多版本按 effectiveFrom 排序，
    `resolve(player, fightTime?)` 按战斗日期选当时有效的知识版本（防"拿当前机制分析历史日志"）
  - `src/data/`：BEAST_MASTERY_KNOWLEDGE（BM，specId 253）+ ARCANE_MAGE_KNOWLEDGE（奥法，specId 62），
    阈值/技能 ID 从旧 constants+rules 抽取（source: manual/verified），优先级规则标 community + 0.6~0.8
  - 接线：BM/奥法 analyzer 的 metrics 现在带 knowledgeVersion/knowledgePatch/knowledgePriority
    （analysis-engine 新增对 @wcl/spec-knowledge 的依赖）
- 踩坑记录：registry.ts 在 src 根，import types 应为 `./types.js` 不是 `../types.js`；
  3 个测试失败全是测试自身 bug（未清 specId、未注册当前版本、0.6 边界断言反了），非源码问题。
- 验证：11 个包 build Done、typecheck OK、36 文件/189 用例全绿。
- 遗留：旧 `specs/*/constants.ts` 仍驱动阈值规则（知识与规则未合一），Phase E/F 处理；
  kill-command 等规则尚未迁移到 Verdict 五档。

## 阶段 D 补记 — 用户提供真实奥法优先级，更新知识数据

- 模型扩展（`packages/spec-knowledge/src/types.ts`）：
  - `AbilityKnowledge.abilityId` / `BuffKnowledge.abilityId` / `CooldownKnowledge.abilityId` 改为**可选**
    （`undefined` = spell id 待核对，指向它的规则自动休眠，绝不编造游戏 ID）
  - 新增 `StackPredicate{key,min,max}`（含 `buffStacks: []` 条件），语义**闭区间**：
    齐射=25 → min/max 25；齐射<12 → max 11；充能<3 → max 2
  - 新增 `KnowledgeNote{id,title,content,source,confidence}`（承载起手/引导补偿/窗口微操等非
    Condition→Action 型知识）；`SpecKnowledge.notes?`
- 奥法知识 v1.1.0：priority 换成用户攻略的 9 条真规则（奥术之魂→弹幕、齐射<12→飞弹、套装增伤→棱彩飞弹、
  齐射=25/≥12+节能/≥12+宝珠+目标≥3→弹幕、充能<3→宝珠、奥冲默认、没蓝→弹幕），notes 存 5 段细节
  （两种起手 / 2连飞弹引导补偿 / 涌动最后2秒不打弹幕 / 进奥术之魂打断飞弹）。
- **待用户补 spell id**（现在为 undefined，规则休眠）：奥术之魂 arcane_soul、齐射 volley、棱彩飞弹
  prismatic_missiles、宝珠 arcane_orb、套装增伤 set_bonus_buff、涌动 evocation。
  提供后用真实日志核对再填，confidence 才能按"verified"抬升。
- 验证：typecheck OK，全量测试绿（spec-knowledge 2 文件全过，新增条件引用完整性/volley 边界编码/
  休眠规则审计等 4 个测试）。

### 奥法 spell id 已由用户补齐（v1.2.0）

- 映射（用户 2026-09 提供，标记 PLAYER_GUIDE/manual、confidence 0.9，待真实日志复核）：
  arcane_soul=451038（奥术之魂）、**arcane_salvo=384452（齐射=Arcane Salvo 核心资源/天赋机制，
  叠层计数器 stacks:true）**、prismatic_bolt=1295924（棱彩飞弹）、arcane_orb=153640（宝珠）、
  **tier_4pc_damage_buff=1242974（12.1 四件套"下一次技能增伤"buff）**、arcane_surge=365350（涌动）。
- **关键坑（用户明确提醒，已加防回归测试）**：384452 与 1242974 **都叫 "Arcane Salvo"**，必须拆成两个
  BuffKnowledge 对象（一个核心资源叠层，一个套装增伤 buff）；spell id 是唯一判别键，禁止按英文名匹配。
- 规则 key 同步改名：volley→arcane_salvo、set_bonus_buff→tier_4pc_damage_buff、prismatic_missiles→
  prismatic_bolt、evocation→arcane_surge；知识 patch 标 '12.1'；9 条 priority 动作全部已解析到 spell id
  （仅 buff arcane_charge 仍待 id，测试断言 pendingBuffs === ['arcane_charge']）。

### 奥法 AOE 场景语义（用户修正）

- 第 4c 条（齐射≥12 + 可用宝珠 + 目标≥3 的弹幕）是 **AOE 专属优先级**：单/双目标场景下
  整条删除，不是"条件不满足就不命中"。
- 模型：`PriorityRule.scenario?: 'st' | 'aoe'`（未标 = 两场景通用）；BM 的 beast_cleave_aoe 同步标 aoe。
- 测试不变量：凡 scenario:'aoe' 的规则必须声明 targetCountMin ≥ 3（防误伤单体）。
- 引擎（Phase E）将先按目标数判定场景 → 裁剪规则集 → 再逐条求值。

### Phase E 优先级求值器完成（analysis-engine/src/priority/）

- 新增模块：`types.ts`(已建) / `knowledge-index.ts`(id/key/cooldownMsByKey 查找表) /
  `state.ts`(事件重放 → ObservedDecision[]) / `evaluator.ts`(Condition→Action + 五档 Verdict) /
  `index.ts`（经 analysis-engine index.ts 导出，**未接入默认 SpecRegistry**，兼容性保留）。
- 五档语义（authoritative）：correct=命中最高确定性规则；suboptimal=更高确定性规则可触发却打了
  更低合法规则(需 expected.confidence≥0.6)；acceptable=同上但 expected<0.6 降级；mistake=expected
  确定性触发且实际行为无 fireable/indeterminate 规则可解释(受 canEscalateToMistake 门控)；
  unknown=expected 无法确定/状态不可观测/被 indeterminate 规则可解释/expected 太弱。
- **场景裁剪实现**：scenario:'aoe'/'st' 规则在目标数可观测且场景不符时**整条删除**(gate='out'，不能
  fire 也不能 explain，先 filter 再求值)；目标数不可观测时(scenario='unknown')场景锁定规则只能作
  indeterminate 解释(unknown)，绝不能产出 confident verdict —— 与"AOE 规则单/双目标删整条"的用户要求一致。
- state.ts 决策点：begincast=有施法条技能的一次决策，其完成 cast 不再算决策；瞬发只有 cast。
  叠层 buff 用 ±1 事件近似（applybuff/applybuffstack +1、removebuff/-stack 减、refresh 不叠层），
  WCL stack 字段未进入内部模型(已知偏差,文档化)；cooldown 以 begincast 或 cast 起算；
  resource=cap 已知时百分比(0..100)，Mana 无 cap → undefined → 条件 unknown(诚实降级)；
  targetCount=滚动窗口内 distinct 敌方 damage target(默认 5s，targetIsFriendly 排除)。
- 测试：tests/priority-state.test.ts(9) + tests/priority-evaluator.test.ts(13)，共用
  tests/priority-fixtures.ts 合成知识。奥法集成用例断言 ST 下 expectedRuleId ≠ arcane.barrage_orb_aoe。
- 全链路验证通过：analysis-engine typecheck ✓ build ✓ lint ✓ 全 91 tests ✓（22 新增），全仓 pnpm typecheck ✓。

### Phase F Finding/Evidence 升级完成

- **domain Evidence 增 `expectedAt?: number`**（期望 vs 实际时间点对，可选、纯增量）；domain 需先 build
  其它包才读得到新类型。
- **core/finding.ts**（analysis-engine）：`severityForVerdict`（mistake→conf≥0.8?high:medium；
  suboptimal→medium；acceptable→low；correct/unknown→info；引擎策略非知识）、`evidencePoint`（丢弃
  undefined、兼容 exactOptional）、`comparisonFinding`（无 severity 时由 verdict 推导）。
- **规则层回填**：makeCooldownDelayRule 增可选 confidence + evidence 带 expectedAt(真实理想槽位
  start+slot*CD，勿用 timestamp-delay——提前开会被钳 0 错位)/fightId/abilityId；makeGcdIdleRule
  evidence 补 fightId + expected/actual。BM 5 条+奥法 4 条规则补 confidence（取自 spec-knowledge
  abilities/buffs/cooldowns 查找，不硬编码）/expected/actual；BM cooldown-delay、gcd-idle 两自建实现
  收敛为工厂复用。clearcasting-waste 的 proc evidence 加 12 点均匀采样上限。
- **blood/elemental 未标 confidence**：无 spec-knowledge 锚点，硬标违背"知识唯一来源"，待 D 期知识扩展到
  这两专精再补（AGENTS 已注明）。
- **priority/verdict-findings.ts**：DecisionRecord 按 expectedRule|verdict|actualKey|acceptableRule
  聚簇 → 1 Finding；默认只出 mistake/suboptimal/acceptable；evidence 均匀采样 cap(默认8)。
- **priority/rotation.ts**：evaluateRotation(context) = createDefaultRegistry().resolve(player,
  fight.startTime) → replay → evaluatePriority → 转 Finding（未接入默认 SpecRegistry，供 H 期接线）。
- 验证：engine typecheck/lint/build ✓；engine 107 tests ✓（新增 finding 6 + finding-upgrade 4 +
  priority-rotation 6 = 16）；全仓 typecheck ✓；pnpm test 全仓 ✓（含 mcp/web/application 无回归）。

### Phase G AI Reasoning 包 + 结构化输出完成（packages/ai-reasoning）

- **新包 @wcl/ai-reasoning**（deps: @wcl/domain + zod；零 analysis-engine/application 依赖——输入用
  结构类型 BriefInput 承接，调用方传 AnalysisResult/RankingReference 天然满足）。
- 模块：`types.ts`（AnalysisBrief/BriefInput/BriefOptions/BriefMeta/BriefVersions）/
  `brief.ts` buildBrief（确定性输入裁剪：severity 权重 critical..info 稳定排序→slice(maxFindings=24)；
  evidence 采样(6)/描述截断(220+…)/recommendation 截断(160)；metrics 递归两层展平只留有限标量+短标量
  数组(key 排序、容量 40、数组截 12)，丢弃 NaN/对象数组/空数组；reference 去 top[] 整列、stats 只留
  有限数值、player.percentilePct 钳 0..100；sources=保留 finding id 列表）/
  `schema.ts`（CATEGORY_VALUES `as const satisfies readonly FindingCategory[]` 编译期同步；AiReport=§23
  形状 summary/performance.score/findings[{priority,category,title,reason,impact,solution,sourceId}]
  max6/recommendations max5；parseAiReport 剥 ```json 围栏+散文取最外层 {…}→JSON.parse→zod，失败给
  findings.0.category 式路径；AI_JSON_ERROR_PREFIX='JSON_ERROR:' 识别降级出口；checkProvenance 校验
  sourceId ∈ brief.sources + priority 唯一）/
  `prompt.ts`（SYSTEM_PROMPT：explain-not-compute、sourceId 必引、verdict 5 档语义+0.6 门控、reference
  percentile 直接引用、JSON 输出契约、JSON_ERROR: 逃生门；全文无数值/技能 ID 硬编码，有测试锁死）/
  `index.ts`。prompt 不重导出 schema 常量（避免双 `export *` 同名冲突，直接字面量）。
- **类型坑**：`Required<{a?: number|undefined}>` 属性类型仍含 undefined——默认值改用
  `Record<keyof BriefOptions, number>`。
- **web 最小接入**：apps/web package.json 增依赖；server.ts buildAnalysisUserMessage 改
  buildBrief(maxFindings 16/maxEvidence 4)+buildUserContent——用户消息从全量 findings+metrics 换成
  有界 brief（token 裁剪）。系统提示保持教练式 markdown（未切 ai-reasoning 的 JSON 契约，避免破坏
  前端流式渲染；结构化回复接线留给 MCP/前端）。web prompt.ts 未动。
- 验证：ai-reasoning 30 tests（brief 12/schema 12/prompt 6）✓ lint ✓ build ✓；web 31 tests ✓；
  全仓 pnpm typecheck ✓；pnpm test 12 工程全绿 exit=0。workspace 13 工程（+ai-reasoning）。

### Phase H MCP 工具对齐完成

- **application/analysis-views.ts**（新）：`buildCombatFactsView`(FactSet→有界视图：top20 技能按次数降序、
  idle 窗口采样5、cooldown delay 采样6 且都带 capped 标志、buff/resource 全量小表)、`buildRotationDigest`
  (PriorityEvaluationResult→scenario/breakdown/decisionCount/mistake+suboptimal+acceptable 每类采样4 +
  samplesCapped、knowledge 版本)。
- **AppService 新方法**：`getCombatFacts`(buildCombatFacts+view,sourceId=targetId=playerId,cooldowns 透传)、
  `getSpecKnowledge`(knowledgeRegistry.resolve(player, fight.startTime)，无可用知识 throw 明确中文错误)、
  `analyzeFight`(loadCombatContext → runAnalyzers + evaluateRotation 判定 findings 并入 prioritize 重算
  score → buildReference → +rotation digest；analyze_player 保持原行为)。**关键重构**：analyzePlayer 与
  新方法共享私有 loadCombatContext（原重复 fetch 块收敛）。
- **裸事件限流**：getPlayerCasts/Buffs/Damage/Deaths 增可选 {limit} 第 4 参，服务端 clampEventLimit [1,
  MAX_RAW_EVENT_LIMIT=500]，防原始事件进 LLM 的通道泛滥（审计缺陷之一）；analyzePlayer 内部 loadPlayerEvents
  不走 clamp（不影响分析质量）。
- **MCP tools**：新增 tools/insight.ts（get_combat_facts / get_spec_knowledge / analyze_fight）；player.ts
  事件 tool 加 limit 字段（optional max 500，测试确认缺省传 {limit:undefined}→服务端默认 500）；index.ts
  注册 registerInsightTools。
- **坑**：MCP 依赖 application 的 dist 类型——改完 app-service 需先 `pnpm --filter @wcl/application build`
  再 mcp typecheck，否则读到旧 .d.ts；zod 字段忘 `.optional()` 会变 Required 导致 SDK 校验报
  "Required at limit"；exactOptionalPropertyTypes 下工具 handler 要条件赋值（同 analysis.ts 模式）；
  测试 mock 未同步 override getFightFriendlies 时 spec 名会被 actor 合并覆盖（Demonology 用例一度仍解析
  到 BM）。SDK 超限参数在 client.callTool 层返回 isError（非 throw）。
- 验证：application 13 用例 ✓（新增 limit 钳制/facts 视图/知识解析/analyzeFight）；mcp-server 11 ✓（新增
  tools-contract 8）；engine 107、ai-reasoning 30、web 31、其余不变；全仓 typecheck ✓；pnpm test exit=0。

### Phase I 双版本号 + 持久分析缓存完成

- **analysis-engine/types.ts**：`export const ANALYZER_VERSION = '0.1.0'`（注释要求与 package.json
  version 同步、bump 即缓存失效）；`AnalysisResult` 增可选 `versions?: AnalysisVersion`（domain 已有类型）。
- **storage cache-repository**：`getAnalysis/setAnalysis` 增第 4 参 `versions: AnalysisCacheVersions`
  {analyzerVersion, knowledgeVersion?}（storage 本地接口，不引 domain）；key=
  `analysis:v{ANALYSIS_VERSION}:{encodeURIComponent(analyzerVersion)}:
  {encodeURIComponent(knowledgeVersion ?? 'none')}:{report}:{fight}:{player}`。
  **语义变化**：ANALYSIS_VERSION(整数) 从"算法版本"降级为"缓存结构版本"，内容失效由双版本 key 自动达成——
  任何规则集/知识升级 → 新 key → miss → 自动重算，无需手动失效。encodeURIComponent 防版本含 ':'。
- **AppService**：构造器增可选 `analysisCache?: Pick<WclCache,'getAnalysis'|'setAnalysis'>`（不传行为不变，
  18 既有用例零改动通过）；私有 `buildAnalysisVersion(player, fightTime)` = registry.resolve → knowledge
  undefined 则 versions 只含 analyzerVersion；analyzePlayer/analyzeFight 改缓存两段式：read（命中直接返回，
  跳过 runAnalyzers + buildReference 网络）→ miss 计算并统一回填 `versions` → write。analyzeFight 的
  evaluateRotation 提前到函数头只跑一次（miss 用于 merge findings、命中时重算 digest 复用同一结果）。
- **web/mcp buildContext**：`new AppService(client, database.cache)` 启用 SQLite 持久分析缓存
  （database.cache: WclCache | undefined 兼容）。
- **坑**：改 analysis-engine/storage 公共签名后必须先 build 这两包再 typecheck 下游（读 dist .d.ts）；
  现有测试断言全字段级访问 → result 加 versions 无破坏；BM/奥法知识 effectiveFrom 2026-01-01 → 断言
  knowledgeVersion 必须用 datedClient 的 knowledgeLiveDate fight（fight.startTime=0 解析不到知识）。
- 验证：storage 18 tests（+3 版本隔离：analyzerVersion 变/knowledgeVersion 变/缺省 'none' 桶隔离）；
  application 18 tests（+5：versions 回填 BM=1.0.0/Demonology 缺省、命中不重拉 rankings、analyzeFight 命中
  仍产 digest、无缓存时全算）；全仓 typecheck ✓ pnpm test 12 工程 exit=0。

### Phase J 测试重构收官（A–J 全部完成）

- **固定黄金 fixture**：`scripts/generate-combat-fixtures.mjs`（可复现生成器，勿手编 JSON）→
  `tests/fixtures/combat/{hunter-bm-basic,mage-arcane-basic}.json`。JSON 结构：meta（id/specName/
  specId/knowledgeVersion/fightDateIso/durationMs + expectedFindingIds 或 expectedRotationFinding——
  作者意图=黄金断言）+ report/fight/player 常量 + CombatEvent[]（绝对时间戳，fight.startTime=2026-06-01
  落在知识生效窗口内保证 registry.resolve 命中预期版本）。BM 场景：前 30s 1.5s/cast（KC 仅 0/6/12/18s）
  后 30s 挂机 → 恰好 3 finding（kill_command_usage/gcd_idle/barbed_shot_uptime 均 high；beast-cleave
  因无 multi-shot 事件不适用）。奥法场景 = 引擎单测同款：salvo 12 层 + t+20s missiles → rotation
  mistake arcane.orb_low_charges。
- **integration 工程**：`tests/integration/`（@wcl/integration private，devDeps workspace:* 各包 + vitest；
  pnpm-workspace.yaml 加 'tests/integration'（workspace 14 工程），vitest.workspace.ts 加
  'tests/integration/vitest.config.ts'）。tsconfig extends 根（resolveJsonModule 已开但用 node:fs
  readFileSync(new URL(rel, import.meta.url)) 读 fixture，避免 import attributes 兼容问题）。
  tests/fixtures-sanity.test.ts（8：meta/fight 自洽、时间单调严格递增、type ∈ 合法集合、fightId/sourceId
  一致、meta 意图标记存在）+ tests/analyze-fight.integration.test.ts（3：makeClient 按事件 type 过滤喂
  7 类 feed + boss:1000 + getEncounterRankings vi.fn 计数——BM e2e 断言 3 finding id + versions
  knowledgeVersion=1.0.0 + 二次调用缓存命中 rankingsCalls 仍 1；奥法断言 rotation digest
  scenario st/knowledgeVersion=1.2.0 + findings 含 verdict mistake expected.ruleId=arcane.orb_low_charges）。
- **坑**：pnpm workspace globs 只 apps/* packages/* —— 新工程须加进 pnpm-workspace.yaml 才被 -r 识别
  （Scope 13→14）；Finding.expected 是开放对象类型，断言其内部字段需 `as { ruleId?: string }` 收窄；
  integration 依赖各包 dist，fresh checkout 需先 pnpm build（与全仓既有惯例一致）。
- 验证：integration 11 tests ✓ typecheck ✓ lint ✓；全仓 pnpm typecheck ✓、pnpm lint exit=0、
  pnpm test 13 工程全绿 exit=0。
- **A–J 全部收官**。AGENTS.md 已加遗留 backlog：death 分析空 / debuffs 未拉取 / MCP 原始事件已限流 /
  metrics.spec override 风险（audit §10 真实缺陷）。
